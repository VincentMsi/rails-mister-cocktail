<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url( <%= image_path("#{@cocktail.banner_url}") %> );">

  <div class="container">
  </div>
</div>

<h1> <%= @cocktail.name %> </h1>
<h1> <% @cocktail.doses.each do |dose| %> </h1>
  <h1>  <%=  dose.description %> </h1>
  <h1> <%= dose.ingredient.name %> </h1>
  <p><%= link_to 'Delete',
cocktail_dose_path(dose),
  method: :delete,
 data: { confirm: 'Are you Sure?' }  %>

  <% end %>
<%= link_to 'Add ingredients', new_cocktail_dose_path(@cocktail) %>
